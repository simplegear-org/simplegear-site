{
  "scripts": {
    "generate": "node scripts/generate-config-artifacts.mjs",
    "precheck": "npm run generate",
    "check": "node scripts/validate-site.mjs && node scripts/test-deep-link-routing.mjs",
    "validate": "npm run check",
    "build": "npm run check"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {}
}
